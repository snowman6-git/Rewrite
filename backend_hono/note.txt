/tokenize 엔드포인트로 텍스트 => 토큰수로 치환가능함 차후 쓸일있을거 같음

일단 세션기반채팅에선, for문 써서 가장 최신 기억을 desc 쿼리로 될때까지 보내볼 생각임 ㅇㅇ 유사 브루투포스 말고 토큰수 세기로 하면 좋긴할듯


예상 가능 취약점

1. 지금 채팅 요청을 url의 book_id기반으로 하니까, 프론트 위조시 다른 채팅방에(uuid라 힘들지만 그래도) 덮어써버릴수 있음
해결법: 세션또한 uid기반으로 접근 가능한 유저를 제한검 < 어차피 다른 이유때문에도 해야함 ㅇㅇ

page_id 를 테이블로 쓰는것도 괜찮아보임
-> 방 지울때 지울게 너무 많음. 채팅만이라도 그ㅜ렇게 나눠놔


book의 최소 요건

id(uuid) / 제목

id가 있어야 호출과 조인이 되고, 아무리 불러오는게 적어도 제목은 필요함
하지만 이는 메인 테이블이 되서는 안됨, 책장에서 제목만 볼 수 있는 그런 상황을 테이블로 만들자.

해당 테이블 호출 예상 타겟 

[채팅세션(타이틀만 필요함), 서재(활성화 된 세션이 어떤 책인지)]

채팅세션: 세션에는 뭘로 기록할거냐?

1. 일단 모든건 id로써 동작할거임, 제목이 바뀌었을때 따라갈 수 있는 primary로써 동작해야함,  ID를 가졌다면, 해당 책의 어떤부분도 조인할 수 있어야함
= 책 관련 모든 변수는 ID를 가져오거나 하드 클론한다.

서재의 경우 열린책 책ID, 고유 ID를 시작으로 이것저것 넣는식

A_?
B_user_idpw

CREATE TABLE IF NOT EXISTS B_Bookspine (
    id TEXT UNIQUE NOT NULL
    title TEXT UNIQUE NOT NULL
)
CREATE TABLE IF NOT EXISTS C_Bookshelf (
    id TEXT UNIQUE NOT NULL,
    cover BLOB,
    desc TEXT NOT NULL,
    system TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS D_Starting (
    id TEXT NOT NULL,
    point_id TEXT NOT NULL,
    name TEXT NOT NULL,
    content TEXT NOT NULL,
    UNIQUE (id, point_id)
);

Bookspine [id, title]
                    [id(로 타이틀 조인), session_id] Book
                                      Pages [session_id(를 기준으로 세션에 대화 로드)]

여기서 논점은, 시스템 프롬프트랑 커스텀 프롬프트를 따로 저장할것인가임

시스템 프롬프트: 매턴마다 적용되야함
커스텀 프롬프트: 매턴마다, 하지만 없을수도 있음

Pages에 빈칸으로 두자
- 조인이 너무 많음, 그냥 채팅의 일부분으로 치고, 메모리에선 해당 부분 건너뛰고 보내주면 됨, API호출때도 1번메세지 + 최근 30개 이러면 끝
= 정적 위치 지정을 하자
장점: 세션 삭제시 정리 편함, 쿼리문 단순
단점: 문제점은 있는거 같음, 단순생각만 해도, 근데 그게 뭔지 나는 잘 모름

따로 테이블을 가지자
- 시스템 프롬프트 '만' 호출 해야할 수도 있음, 정적 위치호출방식은 위험한거 같음(이유는 잘모름, 정규화관점에서 그런듯)
- 색인에서 압도적인 장점임, 채팅이 많아지면 첫번째 호출이라는거 자체가 오버헤드같음, 업데이트는 말할것도 없지
장점: 테이블 분리를 통한 색인, 조회의 이점(미미할수도있음)
단점: 채팅을 보내기 위해 조인이 또 필요해짐(뭐 그리 복잡하진않음)

대충

테이블 셀렉트 최초메세지 + 최근 메세지 30
(혹은)
테이블 셀렉트 최초메세지
테이블 셀렉트 최근 메세지 30

vs

테이블 셀렉트 최근 메세지 30
테이블 조인 기준 id 셀렉트 시스템프롬프트

페르소나를 생각 못했네, 걔는 어떻게 분리할거임?
ㄴ 여기선 캐릭터로
    ㄴ 근데 이거 캐릭터는 너무 헷갈릴 여지 있지않음? 시장 따라가는것도 필요함 홍대병새끼야
        ㄴ 일단 팩트는 등장인물의 추가로도 사용하긴할거라는거지, 책의 등장인물 개념으로 가는게 목적임, NPC도 추가 가능하게 할거거든
        ㄴ 고민해보자, 기존 시장에서 사용하던 용어의 변경을 하는것에서 오는 약소한 착각과, 차별화 된 이름의 이점이 있는지
        ㄴ 애초에 다른기능으로 봐야함
        ㄴ 중단하고, 나중에 하셈

C_Bookshelf에 system프롬프트가 있긴함
애를 어떻게 할꺼임?
1. 하드클로닝
- 장점: 
- 단점: 
2. 참조
- 장점: 단순해짐
- 단점: 시스템 수정이 안됌

이러나 저러나 결국 월드에딧이라는 시스템을 위해서는 하드클로닝 해야하는게 사실임
이걸 어디에 둘것인지를 생각해야함.



H_Stage[세션_id, 캐릭터_id, 캐릭터_이름, 캐릭터_로어, is_NPC]
= 해당 책에서 등장하는 인물만 

I_Header [세션_id, 하드클로닝 시스템 프롬프트, 커스텀 프롬프트(Nullable)]
ㄴ 저 위에서 논의한 NPC추가는 어떻게 할꺼임?
ㄴ 일단 기능 구현하고 차차 생각하자, 나중에 등장인물로 따로 테이블 만들어도 늦지않음
ㄴ 할거면 지금하고 < 애초에 페르소나 지금없다.
ㄴ 할거면 지금하는게 맞음

CREATE TABLE IF NOT EXISTS I_Header (
    session_id TEXT NOT NULL,
    system TEXT NOT NULL,
    rule TEXT,
);
커스텀 프롬프트 = 룰로 할지 고민하기
역할은 기존 프롬프트를 덮어 쓰거나, 추가할거임
월드에딧엔 원본 시스템 프롬 들어갈거임


J_Characters[캐릭터_id, 캐릭터_이름, 캐릭터_로어, ]
= 순수 캐릭터 저장만

// 일단은 임시로 간단하게, 나중에 정렬법이나 생성일자는 넣을지 말지 고민하기
CREATE TABLE IF NOT EXISTS J_Characters (
    id TEXT NOT NULL,
    name TEXT NOT NULL,
    lore TEXT NOT NULL,
);

채팅 한번에 조인 ㅈㄴ 많겠네
근데 그걸 알아야하는게, 개별적 요청도 필요하긴해서 그럼, 어차피 페르소나 테이블 따로 있을예정임

이러면 unfold의 순서는

Flags
O: out
I: input
int: flag_num

시작 
id기반 원본 책(시스템프롬) O-1
스타팅 복사 O-2
F_Book에 원본id, [uuid4 세션아이디]
I_Header에 [uuid4 세션아이디], 시스템 프롬프트 I-1
G_Pages에 [uuid4 세션아이디], 스타팅 콘탠츠 role system으로 추가 I-2

채팅 요청시
I_Header에서 시스템 프롬프트, (있다면)rule도 포함 [시스템프롬프트]
G_Pages에서 최근 채팅 30개 [히스토리]
H_Stage에서 session_id JOIN -> 캐릭터_id기준 J_Characters조인 -> SELECT name, lore [페르소나, 로어]
ㄴ 커스텀 등장인물 섹션으로 분리될거임(기존의 시스템이 등장인물을 시스프롬에 박는거니까)

